account_id: "999888777666"
environment: "staging"
carid: "600001725"
default_region: "us-east-1"

security_groups:
  broken-sg:
    description: "Intentionally broken SG"
    ingress:
      - protocol: "tcp"
        from_port: 22
        to_port: 22
        cidr_blocks: ["0.0.0.0/0"]
        description: "SSH from the entire internet"
    egress:
      - protocol: "tcp"
        from_port: 443
        to_port: 443
        cidr_blocks: ["10.0.0.0/8"]
        description: "HTTPS outbound"
