account_id: "999888777666"
environment: prod

tags:
  ManagedBy: sg-platform
  Team: platform-test

security_groups:
  api-gateway-prod:
    description: "API gateway ingress from corporate networks"
    tags:
      Application: api-gateway
      Environment: prod
      ManagedBy: sg-platform
      Team: platform-test
    ingress:
      - description: "HTTPS from corporate"
        protocol: tcp
        from_port: 443
        to_port: 443
        prefix_list_ids:
          - corporate-networks
    egress:
      - description: "HTTPS to backend services"
        protocol: tcp
        from_port: 443
        to_port: 443
        cidr_blocks:
          - "10.0.0.0/8"
