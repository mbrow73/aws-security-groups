# AWS Security Group Platform - Example Configuration
# Copy to accounts/<your-account-id>/security-groups.yaml

account_id: "123456789012"
environment: "prod"  # prod | test | dev
carid: "600001725"   # Your application's CARID
default_region: "us-east-1"  # Default region for SGs that don't specify one

# Note: Baseline SGs (EKS, VPC endpoints) are managed by AFT — not this repo.
# Note: Corporate mandatory tags are auto-generated from environment + carid.
#       You do NOT need to specify them per security group.

# Team security groups
security_groups:

  # Example: Application behind NLB (inherits default_region: us-east-1)
  app-backend:
    description: "Backend API service"
    ingress:
      - protocol: "tcp"
        from_port: 8080
        to_port: 8080
        security_groups: ["sg-0123456789abcdef0"]
        description: "HTTP from ALB"
      - protocol: "tcp"
        from_port: 9090
        to_port: 9090
        cidr_blocks: ["10.160.0.0/16"]
        description: "Prometheus scraping"
    egress:
      - protocol: "tcp"
        from_port: 5432
        to_port: 5432
        security_groups: ["rds-postgres"]
        description: "PostgreSQL access"
      - protocol: "tcp"
        from_port: 443
        to_port: 443
        prefix_list_ids: ["corporate-networks"]
        description: "On-prem services via TGW"
      - protocol: "tcp"
        from_port: 443
        to_port: 443
        baseline_ref: "vpc-endpoints"
        description: "ECR, S3, STS, CloudWatch via VPC endpoints"
      - protocol: "udp"
        from_port: 53
        to_port: 53
        cidr_blocks: ["10.0.0.0/8"]
        description: "DNS resolution"

  # Example: Database (inherits default_region: us-east-1)
  rds-postgres:
    description: "PostgreSQL RDS"
    ingress:
      - protocol: "tcp"
        from_port: 5432
        to_port: 5432
        security_groups: ["app-backend"]
        description: "PostgreSQL from app servers"

  # Example: Cache in a different region (explicit region override)
  west-cache:
    region: "us-west-2"
    description: "Redis cache in us-west-2"
    ingress:
      - protocol: "tcp"
        from_port: 6379
        to_port: 6379
        cidr_blocks: ["10.200.0.0/16"]
        description: "Redis from app tier"
