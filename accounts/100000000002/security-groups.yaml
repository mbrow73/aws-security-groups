account_id: "100000000002"
environment: "dev"
carid: "600001725"
default_region: "us-east-1"

security_groups:
  clean-test-sg:
    description: "Perfectly valid SG - no warnings no errors"
    ingress:
      - protocol: "tcp"
        from_port: 443
        to_port: 443
        cidr_blocks: ["10.160.0.0/16"]
        description: "HTTPS from internal network"
    egress:
      - protocol: "tcp"
        from_port: 443
        to_port: 443
        cidr_blocks: ["10.0.0.0/8"]
        description: "HTTPS outbound to internal"
