# Known Prefix Lists — legacy compatibility file
#
# Prefix list ownership is now decoupled from the baseline model.
# Shared/self-service prefix lists should live in shared-prefix-lists.yaml.
# Keep this file only if you still need temporary static/manual mappings during transition.

known_prefix_lists:
  - dynamodb
  - s3
